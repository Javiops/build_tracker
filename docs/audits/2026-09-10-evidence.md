# Evidence audit — 2026-09-10

**Finding: the gold feature passes its causal test, but the entire input state does not.** In a synthetic pair, a decision at 150s with purchases at 150s and 155s acquired a different `kills` input when a kill was inserted at 153s. Labels and `gold_est=735` stayed identical; query index 4 changed from 0 to approximately 0.1. This reproduces a contract violation, not its frequency or effect on real scores.

Source: `data/audit_reconstruction_probe_20260910.py`. The path is `app/reconstruct.py:close_visit` (`end_frames`, `end_kda`, `end_objectives`) → `scripts/baseline.py:_example` → `scripts/train_prefix.py:ShopDataset._fill_row`. The shopper's board snapshot uses start state, but `_example` takes kills/deaths/level from the event. Objectives follow the same end-state path; only kills were dynamically demonstrated in this audit. No production code was changed.

## What the ledger actually supports

- **3k probe: report and artifact hashes confirmed; not rerun.** `first_action_exact`, validation, 591 games / 69,857 visits: A 0.32031149, B 0.20374479; paired difference 0.11656670, CI95 [0.11308035, 0.11974225]. Both report zero over-budget recommendations relative to their respective offline budgets. A digest `ff692a974895d5af43662ce393cefeca92c326130ec5773ee933ed2140015df4`; B `dfc90a8897b2fce7b7be5705403457745d5427e22f875d7c42ba1a46ddda5515`. Both are `probe_only` and share export fingerprint `cda2cdb3599a25f5`. Source: `data/ml/probes/causal_v2_newest_3000/policy_eval_val_probe_candidate_prequential_vs_probe_candidate_stale.json`. The prompt's rounded values agree. The stronger claim of fully causal input is contradicted above. This comparison changes training budget and decoding budget together; it does not isolate what the network learned.
- **Spend-pricing claim: unconfirmed.** No source report or matching entry was located in the searched local JSON reports and logs for the quoted 92.6% wallet identity result. Sample size and artifact/source binding are absent from the prompt. Inadmissible as a freshly verified result. Recipe fixture tests pass; that does not reproduce the corpus statistic.
- **Zero residual flicker: unconfirmed historical statistic.** The script exists, but no matching saved result was located for the claimed 300-snapshot run. Do not restate it as verified live performance. The current product computes advice in explicit sessions, not continuous ranking.
- **Save-card precision confirmed as report content:** 0.09803922 over 5,304 displayed-save visits, validation, same 3k report and A digest above. This combines lead and alternative cards; it does not establish that 90% of lead save advice is wrong.
- **Conditional comparison has weaker provenance.** `data/ml/probes/causal_v2_newest_3000/conditional_policy_val.json` records `first_action_exact=0.20779593`, validation, n=69,857, but contains no export fingerprint, artifact binding, game list, or paired uncertainty. Matching counts alone do not certify pairing.
- **Newer evidence omitted by the prompt:** `first_action_exact=0.35071674`, CI95 [0.34857690, 0.35275258], validation, 1,980 games / 240,114 visits, digest `7660ca0206cafd583c8e002bd8fd305c330d101fd69f4be05861b85ead5ccd84` (hash checked). Source: `data/ml/probes/causal_v2_newest_10000/policy_eval_val_probe_candidate_prequential.json`. Still `probe_only`; different export/population, no stale comparator in that report. It is not evidence of improvement over the 3k run or live quality.

## Checks, operations, decision

All eight requested scripts exited 0 on CPU. Full verbatim output is in `docs/audits/2026-09-10-phase0.txt`; machine-readable output is in `data/phase0_audit_20260910.json`. The split-redraw banner belongs to temporary fixtures. The live-predict test used the legacy artifact and four saved snapshots; it does not certify deployment eligibility.

Task Scheduler reports **BuildTracker Causal Recovery: Running**; daily pull and both probes are Ready. The current full-refresh attempt started at 18:26:12 per `data/causal_recovery.log`; `data/backfill.log` was still advancing. The supervisor already queues verification, gap-only catch-up, another refresh, export, three trunk/graft pairs, and validation reports. Nothing was stopped or added to that chain. No Riot calls, production export, training, promotion, or final-test read was launched by this audit.

**Cancel recommendation:** defer model selection from the queued ablation until start-state correctness is resolved. **Next:** pin every input to decision start, add a whole-input no-future property, and measure affected stored rows before selecting a repair route. Strongest counterargument: affected visits could be rare enough to have negligible metric impact. That effect is unmeasured; rarity does not make future information legal. Fixture work needs no API/GPU time. Corpus repair cost is unknown until cache coverage and reconstructability are checked; do not prescribe another full API crawl. The running supervisor will not automatically observe this recommendation. Changing its queued stages requires a deliberate operational decision.
