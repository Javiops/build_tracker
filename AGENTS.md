# AGENTS.md

Read [`CLAUDE.md`](CLAUDE.md). It applies to Codex and every other assistant
identically — the same project contract, prohibitions, commands and gotchas.

This file is a pointer on purpose. It used to be a hand-maintained copy of
`CLAUDE.md` that had drifted: it was missing the entire operating contract, so
Codex and Claude were running on different instructions in the same repo.
Do not re-add content here; edit `CLAUDE.md`.
