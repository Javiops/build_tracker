---
description: Plan and record a model or decoder comparison
---

Load the `run-experiment` skill and follow it.

Before anything runs, write down: the question, primary metrics and
populations, the unchanged export fingerprint, candidate names, the stopping
rule, and the resource budget. Select on paired validation differences only.
Never compare across different exports and call the difference an improvement.

Use the full response shape for this work:

FINDING → EVIDENCE (with provenance) → WHAT WOULD CHANGE MY MIND → COST →
NOT DOING THIS CYCLE → RISK → NEXT
